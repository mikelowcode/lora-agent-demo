# Localist Framework — Canonical Architecture Specification

> **Status: Authoritative**
> This document is the canonical reference for Localist Framework's substrate architecture.
> No implementation begins until it is reflected here. No deviation from this
> specification is made without updating this document first.

> **This file is now an index.** Full section content lives in
> `docs/architecture/NN-*.md` (one file per numbered section below). Retired
> sections live in `docs/architecture/archive/`. Read this index first; only
> open the specific section file you need. Edits to substance go in the
> section file, never back into this index — see "Keeping this index current"
> at the bottom.

---

## Core Constraints

Localist Framework is a **local-first, agentic general assistant**. Every architectural
decision is evaluated against five constraints:

| Constraint | Meaning |
|---|---|
| **Local** | All inference, embeddings, memory, and tools run on-device. No cloud calls except explicit user-initiated web search or page fetch. |
| **Sparse** | Memory is high-value semantic events, not transcripts. Prompts carry only what is needed. |
| **Predictable** | The same input produces the same routing decision. Inference is used for reasoning, not for control flow, except where explicitly specified. |
| **Minimal** | System prompts are small. Persona lives in the wiki. Agents are single-purpose. |
| **Auditable** | Every prompt can be logged and read. Every memory write has provenance. Every routing decision has a named rule. |

These constraints are not preferences. They are the identity of the system.

---

## Sections

| # | Section | Status | Last updated | Summary |
|---|---|---|---|---|
| 2 | [Episodic Memory Schema](docs/architecture/02-episodic-memory-schema.md) | Authoritative | 2026-07-31 | Defines the episode as the unit of durable memory — a sparse, typed, provenanced fact extracted from conversation (not a transcript or turn log) — plus its table schema, field reference, type taxonomy, lifecycle rules, retrieval modes, and summarization contract. New 2026-07-24 (§2.6): Mode 4 — graph-neighbor expansion, part of the new Episodic Injection rule (§4.3a of the Planner Routing Model doc) — after Mode 3 picks a top semantic candidate, its episode graph node's resolved outgoing edge(s) are followed to a wiki concept, and that concept's episode-sourced backlinks are pulled in as further Slot 3a candidates via the new `EpisodicMemoryReader.get_by_ids()`; the existing confidence>=0.7 injection floor is unchanged and still governs what surfaces. Also new 2026-07-24 (§2.13): the Episode Browsing UI's "Related Memory" panel moved off an exact `task_id` match (which read as empty for most turns, since episodes are sparse by design) onto a real Mode 3 similarity lookup — `GET /memory/episodes/related`, `by_similarity()` gained an `exclude_task_id` param so a turn's own episode(s) don't show up in their own results — and the keyword-fallback scoring used wherever no embedding is available (`query_corpus()`'s stage-1 prefilter, and `EpisodicMemoryReader`'s per-row fallback) was replaced with a hand-rolled Okapi BM25 (`backend/bm25.py`) in place of the old Jaccard set-overlap scoring. Since raw BM25 is unbounded, the pre-existing absolute relevance floors tuned for cosine/Jaccard (`controller_agent.py`'s `>= 0.55` RAG-source gate, `by_similarity()`'s `min_score`) no longer meant anything for it — a ceiling-normalization fix was tried and rejected (it capped realistic single-mention matches around 40%, breaking real passing tests), so instead `DocumentResult`/`_score_all_active()` now carry a `scored_by_embedding` flag and BM25-scored results skip the floor entirely (trusting ranking), except `best_match()` (retraction), which keeps its floor unconditional since a false-positive retraction is worse than a recall miss. New 2026-07-23 (§2.12): fixed a live bug where "remember I'm X" (no "that") wrote nothing — two independent gates (`planner._priority2_memory()` and `episodic_extractor.detect_explicit_signal()`) both required the literal "remember that"; fixed with a shared deterministic pattern (`_has_explicit_remember_signal()`) after semantic gating was tried and rejected on measured evidence (write-intent vs. recall-intent collide too much for cosine similarity here, unlike §4.3's Priority 5 fix). Same-day follow-up: closed the "don't forget" retraction collision surfaced above — `_RETRACTION_SIGNALS`' "forget that" substring was matching "don't forget that X" (means "remember") and routing it to delete instead; fixed via `planner._MEMORY_NEGATED_FORGET`, checked before the retraction loop, folded into `_has_explicit_remember_signal()`. Also covers: real cosine-similarity retrieval (Mode 3) merged with recency priming (Mode 2) so all seven episode types are actually reachable, not just the four Mode-2 types; semantic (embedding-based) retraction via `best_match()`/`retract_by_id()`; the `pending` lifecycle status and write-approval gate (§2.11); content-safety scanning on write (§2.10); and the `MEMORY.md` human-readable snapshot (§2.9). New 2026-07-31: the lifecycle gained its one reversal transition, `retracted → active` via `EpisodicMemoryWriter.reactivate()` — supersedes a conflicting newer-active row for the same `(subject, episode_type)` rather than leaving two active, unlike `approve()`'s documented punt on the same edge case; see §20.11 for the endpoint/UI. |
| 3 | [Unified Prompt Contract](docs/architecture/03-unified-prompt-contract.md) | Authoritative | 2026-08-03 | New 2026-08-03: Slot 1a's assistant name is now a user-configurable setting (`MemoryManager.get_assistant_name()`/`set_assistant_name()`, default `"Localist"`, changeable via `PUT /settings/assistant-name`), not a hardcoded constant — the identity *template* (`PromptBuilder._SYSTEM_TEMPLATE`, renamed from `_SYSTEM`) is still fixed, only the `{name}` it's parameterized with isn't. `_load_persona()` substitutes the configured name into `wiki/lora-persona.md`'s "You are LORA" line the same way it already swapped in the web-search provider label, and now carries real cache invalidation keyed on the assistant name (`ControllerAgent.invalidate_persona_cache()`) — the stability table's prior "Identity constant / Never" row and the persona-cache-invalidation description (previously aspirational, no code implemented it) are both corrected. A name change is a real, accepted KV-cache-prefix miss on the next request, same cost class as a persona edit. Previously, 2026-08-01: Slot SF's PDF/image note resolved — see §22 (local OCR service, not oMLX's native multimodal support). Specifies the fixed multi-slot prompt layout (current datetime, session files, episodic memory, RAG, tool results, working state, instruction), the aggregate token budget, `PromptBuilder`'s interface, and the ongoing KV-cache / stable-prefix investigation into cache efficiency under the current runtime. Updated 2026-07-25: Slot 5 (`[TOOL RESULTS]`, `_CEIL_TOOL`) raised 500 → 1,500 tokens (worst-case aggregate ~2,850 → ~3,850) — search-tool results were landing shallow/mid-sentence-truncated at the old ceiling stacked on top of each provider's own summary cut; see §14.2 for the corresponding structured-formatting change. Also covers: the unnumbered `[CURRENT DATETIME]` slot (§3.2, Slot DT), added 2026-07-17; and Slot 6 (Working Memory) becoming backend-tier-aware (2026-07-18, see §16.7). Updated 2026-07-19 (§16.11): the local tier's working-memory ceiling is no longer RAM-tiered — it's now `OMLXRuntimeClient.max_model_len − 27,000` (floored at 300), and the local turn-count cap is gone entirely (both tiers are now token-only, `working_memory_limit=None`). Also new 2026-07-19 (§3.7d): `PromptBuilder.build(emit_structured_working_memory=True)` — opt-in, oMLX-only, default-False reproduces prior behavior exactly — omits Slot 6 from the flattened `user_prompt` and instead returns the trimmed working-memory turns as a structured `list[Turn]`, which `OMLXRuntimeClient` now sends as discrete `messages` array entries (mirroring oMLX's own web UI and fulfilling §3.7b's speculative multi-turn direction) instead of flattened text — Ollama/Foundry are completely unaffected. |
| 4 | [Planner Routing Model](docs/architecture/04-planner-routing-model.md) | Authoritative | 2026-08-05 | The deterministic, priority-ordered decision tree that routes each instruction to a tool, retrieval path, or direct-answer route; covers `RoutingPlan`/`ControllerResult` schemas, compound-instruction handling, the tool dispatcher, and Gemma 4B-specific behavioral constraints. New 2026-08-04 (§4.6.2 update): Open Item 2's zero-content-Ollama-completion investigation — Phase 1 (passive instrumentation, `_iter_ndjson_chunks()` now captures `done_reason`/`eval_count`/`prompt_eval_count`/`total_duration` off the terminal NDJSON line and `infer_stream()` warns with full metadata + prompt on any zero-content completion) and Phase 2 (a 68-trial live reproduction sweep, `diagnostics/diag_ollama_zero_content_repro.py`, against the real running Ollama backend) both done. Two distinct findings: (A) a new, 100%-reproducible bug on `gemma4:e4b-mlx` — this model streams a separate `"thinking"` field `OllamaRuntimeClient` never read, so at `num_predict=200` its entire budget went to hidden reasoning and it terminated with zero visible content every single time (24/24). **FIXED same day**: `OllamaRuntimeClient` now sends `"think": false` on every request, confirmed live to eliminate the field entirely (verified against the real client, not just raw `requests`) and confirmed harmless on `gemma4:31b-cloud`; live-in-the-UI streaming of the thinking trace was considered and explicitly rejected to keep the `on_token` streaming contract backend-agnostic (only Ollama has a `thinking` concept — `OMLXRuntimeClient`/`FoundryRuntimeClient` don't). Full suite 1498 → 1501 passed. (B) genuine reproduction of the original bug on the actual production model, `gemma4:31b-cloud` — rare at the documented baseline temperature (0/24 formal, one ad hoc hit under identical conditions), 2/10 at temp=0.70, always `done_reason="stop"` with a small nonzero `eval_count` (real tokens generated, never surfaced as content); no correlation found with prompt length or tool-grounding presence, contradicting the original "no grounding" trigger hypothesis a second time. Root cause for Finding B still not established — status stays OPEN, no code fix attempted yet, existing `ControllerAgent` retry-then-fallback guard still absorbs every occurrence transparently. New 2026-07-30 (§4.6.2 update): the empty-completion guard's retry-then-fallback caught a *new* live occurrence of the zero-content-Ollama-completion bug — this time with real tool grounding already present in the prompt (`hacker_news_search`, §14.13), contradicting the original 2026-07-17 finding's "no grounding" trigger condition. Logged as OPEN, not yet investigated — no diagnostic run, no reproduction rate established; the existing guard already absorbs it transparently either way. New 2026-07-24 (§4.3a): the **Episodic Injection rule** — `controller_agent.py`'s Step 5 (episodic retrieval) now runs on every conversational-agent, non-graph-query turn regardless of `plan.fetch_episodic`, decoupling "should we attempt recall" from "should we show it"; `fetch_episodic` stays computed and reported for audit/UX but no longer gates execution, closing the class of gap where a real matching episode exists but no P5 keyword/semantic signal fired at all. Quality control is unchanged — the existing confidence>=0.7/max-5-bullet formatting contract (§2.7) still decides what surfaces. Bundled with this: Mode 4 graph-neighbor expansion (§2.6 of the Episodic Memory Schema doc). Retraction and the injection confidence floor are explicitly untouched. Previously, 2026-07-23 (§4.3): Priority 5 (episodic-fetch gate) gained a semantic cosine-similarity fallback alongside its existing keyword check, after a live miss ("Help me prepare for the upcoming Claude Impact Lab...") matched no keyword and skipped episodic retrieval entirely — threshold (0.70) and templates picked empirically against the real tuned embedding model, not guessed (`diagnostics/reports/episodic_relevance_semantic_gate_2026-07-23.md`); also corrected a stale comment claiming `_episodic_injected` short-circuits P5 to always-fire, which hasn't been true since P5 moved off inference. Same-day follow-up: Priority 2 (explicit memory-write gate, `_MEMORY_KEYWORDS`/`_priority2_memory()`) had the identical "remember that"-only gap for a different reason — semantic gating was tried here too and rejected on measured evidence (write vs. recall intent share too much vocabulary for cosine similarity to separate; two rounds both found negative-max > positive-min). Fixed instead with a deterministic pattern, `_has_explicit_remember_signal()` (bare "remember", excluding interrogative prefixes/trailing "?"/"i remember" reminiscing), shared with `episodic_extractor.detect_explicit_signal()`'s independent, identically-narrow gate so both agree — full detail and the rejected-semantic-gating data at `diagnostics/reports/explicit_memory_write_gate_2026-07-23.md` and §2.12 of the Episodic Memory Schema doc. That same fix-pass surfaced (and, per follow-up request, also closed same day) an adjacent higher-stakes bug: `_RETRACTION_SIGNALS`' `"forget that"` substring was colliding with `"don't forget that X"` (means "remember") and routing it to delete — fixed via `_MEMORY_NEGATED_FORGET`, checked before the retraction loop in `detect_explicit_signal()`; full detail in §2 (§2.12). New 2026-07-22: **Priority 3-news** (§4.2) — `_NEWS_KEYWORDS` (`"news"`, `"headlines"`, `"breaking"`, `"top stories"`, etc.), routing to the new `news_search` tool (full design at §14.9) ahead of the generic Priority 3 `web_search` branch; `"news"` moved out of `_WEB_SEARCH_KEYWORDS` into the new set to avoid the two priorities racing on the same word. Also new: §4.6.1's Step 3b corpus-fallback check extended with a third clause (`r.tool_name.startswith("news_search")`) so `news_search`'s Brave-fallback provenance rename doesn't silently escape the check — same gap `research` already hit and fixed the same way. Now covers: an explicit-date P3 tool-gate signal (§4.2, 2026-07-17) closing a live gap where a specific-date query produced no tool grounding, and the resulting empty-completion guard + bounded forced-`web_search` retry (§4.6.2) that stops a zero-content Ollama completion from ever reaching the user regardless of what the Planner gate missed. New 2026-07-20: a third P3 keyword signal, `_CHART_KEYWORDS` (§4.2), gating the new `generate_chart` tool (full design at §14.8); `ResponseMetadata`'s `chart` field (§4.4a), present only on a successful chart dispatch. Same-day follow-up: `_CHART_KEYWORDS` gained `"turn this into a ..."` phrasing after live testing caught a coverage gap against an already-validated diagnostic-corpus instruction (§4.2). Also new 2026-07-20: **Priority 0** (§4.2), an explicit `/chart`/`/research` slash-command bypass ahead of every other priority — `/research` in particular forces the research loop regardless of `LOCALIST_RESEARCH_LOOP_ENABLED` (full detail at §18.2/§18.9), tagged `tool_signal_source="slash_command"`, live-verified end to end for both commands. New 2026-08-05 (§4.6.2 update): a scratchpad-feature pilot re-tested the `think:false` decision live on an unrelated question (tool-orchestration/fabrication rate, §8.8 Open Item 11) and reconfirmed it from a cost angle — `think:true` cost ~3.4x mean latency and reproduced the known budget-exhaustion/zero-content failure live (20%/5% of trials); no code change resulted. |
| 5 | [Fetcher Service](docs/architecture/archive/05-fetcher-service-retired.md) | **Retired** (2026-07-03) | 2026-07-03 | The standalone FastAPI microservice (port 8002) that performed URL fetch + readability extraction from project inception through Phase 1. Retired in Phase 2 — its logic was ported in-process into the `fetch_url` MCP tool on `localist-mcp` (§14). Kept for historical reference only. |
| 6 | [Build-Order Checklist](docs/architecture/06-build-order-checklist.md) | Authoritative | 2026-07-09 | The strict, dependency-ordered build phases (memory substrate → prompt contract → planner → controller → episodic extraction → tool dispatcher → final integration) plus running session-progress notes. |
| 7 | [Localist UI](docs/architecture/07-localist-ui.md) | Authoritative | 2026-08-23 | The SvelteKit frontend: routes, provenance bar, episodic memory panel, API proxy, status bar and live-turn/SSE streaming behavior, chat history persistence, and the file browser. New 2026-08-23 (§7.27): the GitHub Watch Feed block gained an "Ask about this" button, at parity with the News Brief (§7.16) and Hacker News (§7.21) blocks — clicking a repo row with a known latest release pins `context.github_repo`/`context.github_tag` (both already supported by `MCPToolDispatcher._run_github_release()`/`_priority3_github_release`, §14.12, just never wired to a frontend caller before) and sends "Summarize the release notes for the latest release of {repo}" into the current conversation. No backend changes required. Same pass, at Michael's request: the §7.26 📌 pin-badge removed from the GitHub block (the underlying `GithubWatchRepo.source` field is unchanged, just no longer shown). Live-verified end-to-end via a headless-browser driver script against a real pinned repo (`ollama/ollama`) — the model's summary correctly named the actual pinned release (v0.32.15) and its real changelog contents, with no console errors. Previously, 2026-08-22 (§7.26): pinned GitHub repos — a way to track a repo's releases in the Live Feed panel's GitHub block by `owner/repo` slug, independent of actually clicking "Watch" on GitHub (which also subscribes to that repo's PR/issue emails). New `stores/githubWatchPins.ts` + a free-text "Pinned GitHub Repos" Settings card (no fixed pool, so a list editor rather than §7.14's chip-grid). Live-verified with three real pinned repos (Ollama, an MLX fork, a Microsoft Foundry repo) resolving real release data end to end. Previously, 2026-08-05 (§7.25): a new logo (monochrome place-marker mark) landed — `static/brand/` asset kit, a real SVG favicon replacing a dead `favicon.png` link (`static/` didn't previously exist), and the sidebar wordmark's bare unstyled "L" replaced with the inlined, `currentColor` mark. Same pass: the five single-letter nav badges (C/M/E/F/S) removed as redundant/clashing with the new mark; the last user-visible "LORA" text (`app.html`'s title/meta) corrected to "Localist"; and, at Michael's request, every remaining `lora-*` localStorage key (theme, sidebar, font size, compose doc, preview panels, runtime backend, settings) renamed to `localist-*` across 8 files, accepting a one-time preferences reset. Previously, 2026-08-05 (§7.24): a client-side-only "Font Size" Settings control (Small/Medium/Large/X-Large, `stores/fontSize.ts` cloning the Theme toggle's `localStorage`+`data-*`-attribute pattern) — the first accessibility-scoped feature, shipped as a standalone control. Scales the 86 of 169 frontend `font-size` declarations already on `--text-*` tokens; the remaining 81 hardcoded literals are a known, accepted gap, with a token-migration design proposed but explicitly deferred (not built) in the same section. Previously, 2026-08-04 (§7.23): **Compose Mode** — a manual, per-turn "Add to document" control (`EditableTurnContent.svelte`) accumulates assistant turns into one draft, edited/saved as a single artifact in a new persistent side-by-side `ComposeDocumentPanel.svelte` (a 4th `#app-shell` grid column, reusing the Previews panel's collapsible-column mechanism, §7.14) — entirely frontend-only, zero backend changes, saving through §7.22's existing `POST /files/generated` flow. `stores/composeDocument.ts` scopes `{active, draft, addedTaskIds}` per conversation, `localStorage`-persisted; appends always land at the end of the current draft, so a hand-edit is never clobbered by a later addition. A real bug caught by live verification (a permanently-visible "added" checkmark overlapping short single-line answers) was fixed before shipping by making it hover-gated like the existing pencil toggle. New 2026-08-03 (§7.22): a per-turn "Save as" control — `EditableTurnContent.svelte` toggles an assistant bubble between rendered markdown and an in-place, full-width editable textarea (Save/Cancel replacing a hover-revealed pencil, top-right, only while editing), saving to a new direct-write `POST /files/generated` endpoint (reuses `mcp_server/file_ops.write_file()`'s sandboxing/never-overwrite versioning; `file_ops`'s sandbox root is now synced to `_state.generated_dir` at startup so it can't silently diverge). Superseded an initial floating-popover cut after live feedback that a small popup was the wrong shape for drafting real edits; a follow-up CSS fix corrected the in-place textarea collapsing into a narrow, content-forced-tall column on long responses (a `width:100%`-vs-shrink-to-fit-ancestor bug), replaced with an explicit `width:600px; max-width:100%`. Editing is export-only — never written back to the live chat turn. New 2026-07-30 (§7.21): the last reserved Live Feed block, Hacker News, goes live — moved to sit between the News Brief and GitHub Watch blocks, with a per-story "Ask about this" button (parity with the News Brief block, backed by the new `hacker_news_search` MCP tool, §14.13) and, applied to all three Live Feed blocks, a new per-block collapse/expand control (`previewBlocks.ts`, localStorage-persisted, defaults expanded — a sibling to the whole-panel collapse store, which defaults collapsed). Live testing surfaced and fixed a real fabrication bug along the way: the model inventing plausible-sounding Hacker News commentary from a bare comment count with no real text behind it — fixed at the tool layer, not here (§14.13). New 2026-07-29 (§7.20): the Previews panel's reserved "🐙 GitHub — Coming soon" block goes live — a GitHub Watch Feed showing the user's watched repos' latest releases (`stores/githubWatch.ts`, structurally identical to `newsBrief.ts`; backend at `backend/github_watch.py`, §7.20 and §14.11), same refresh-link UX as the News Brief block, no chat involvement. New 2026-07-24: §7.19 — collapsed the Daily News Brief's duplicative World/National sections (both returned the same headlines for a given `home_country` — a known limitation documented, then live-reported as real redundancy) into a single `top_stories`/"Top Stories" section anchored to `home_country`, cutting the brief from 6 NewsAPI calls to 5; §7.18 — "Daily News Brief Refresh" no longer opens or seeds a Chat Conversation (removed the `chat_turns`/`conversation_log` writes in `POST /news/brief/open`); it only refreshes `news_brief_cache` for the Live Feed panel now, since §7.16's per-article "Ask about this" button made dumping the whole brief into chat on every refresh redundant noise. New 2026-07-23: §7.15 fixed the Live Feed panel staying stale after "Daily News Brief Refresh" (the panel's store was only ever populated once in `onMount`, which never refires on a client-side route change — fixed by explicitly re-fetching after the refresh call resolves); §7.16 added an "Ask about this" per-article button in the Live Feed panel that sends just that one story into the current conversation for a model-generated detail summary (backend `news_search` URL-pinning at §14.10); §7.17 fixed the Settings "Local area" field appearing permanently stuck on its placeholder text — root cause was the Save button silently requiring an unrelated exactly-3-topics selection before it would persist anything, now surfaced as a disabled-button state with a persistent inline hint instead of a post-click error. Also covers: the "1a — Inline Provenance" desktop UI port (§7.10) — new tokens/light theme, collapsible/resizable sidebar with in-place Chat/Files sub-nav expansion, consolidated single-chip appbar (superseding the prior three-chip layout, §7.6), collapsed-by-default provenance disclosure, card-based Settings, and a real backend-wired "Re-embed Corpus Now" control (§7.10, 2026-07-17) surfacing `POST /memory/reembed` with a `corpus_stale` staleness badge; the Files restructure (§7.11) — listing/upload/ingest moved into the sidebar, plus new file download and two-step delete endpoints/UI; KaTeX math rendering in `MarkdownRenderer.svelte` (§7.12, 2026-07-18) — a deliberate, documented exception to the renderer's prior no-third-party-deps rule, added after live use showed models emitting literal LaTeX source (e.g. `$\rightarrow$`) that the backend prompt pipeline was confirmed innocent of; live-verified by the user against the running chat UI; `ChartRenderer.svelte` (§7.13, 2026-07-20) — Chart.js-based interactive chart rendering for the new `generate_chart` tool (full design at §14.8), colored from this app's own CSS custom properties rather than a separate palette, live-verified end-to-end via a headless-browser driver script; and (§7.14, 2026-07-22) the Daily News Brief UI's header-button-and-hover-popover design retired in favor of a persistent, collapsible right-side "Previews" tab (`previewsPanel.ts`/`PreviewsPanel.svelte`, a new third `#app-shell` grid column) after live use showed the popover too small/truncated for real headlines — plus two real Settings bugs found and fixed along the way: a native browser address-autofill dropdown on the "Local area" field (`autocomplete="off"`), and a genuine Svelte compiler pitfall where mixing a store-subscription read with `bind:value` targets in one reactive block silently disabled typing entirely (root-caused by compiling a minimal repro and reading the generated JS, fixed by replacing the reactive mirror with a one-time `onMount` sync). |
| 8 | [Graph Retrieval Layer](docs/architecture/08-graph-retrieval-layer.md) | Authoritative | 2026-08-05 | The offline wiki link-graph (schema, `wiki_doc.py` shared parser, WikiAgent link validation, resolution rule) plus an extensive dated log of validation runs and open items, several since closed — including a new startup `reconcile_wiki()` mechanism (Open Item 13) that resyncs hand-edited wiki pages and removes orphaned `document_index` rows for deleted files, with a dedicated audit log. §8.9 (2026-07-23): graph now feeds inference, not just write-time — a RAG-assist `[RELATED CONTEXT]` slot surfaces a wiki page's 1-hop neighborhood on ordinary RAG turns, and `episodes` get real `graph_nodes` rows (`node_type="episode"`, synthetic `episode://<id>` doc_path) linked to wiki concepts via the existing P3c stem/token-overlap resolver — no schema migration needed, `node_type` was already free-text. New 2026-08-05 (§8.8 Open Item 11 update): a live pilot tested whether re-enabling Ollama's native `thinking` field reduces the fabricated-tool-call rate — inconclusive (0/20 fabrications in both `think=false` and `think=true` arms, too rare to detect at this N), while confirming a real ~3.4x latency cost and the known budget-exhaustion/zero-content failure mode under `think=true`; not pursued further, mechanism remains open. |
| 9 | [Slot 6A — Structured Working State](docs/architecture/09-slot-6a-structured-working-state.md) | Authoritative | 2026-07-06 | The `[WORKING STATE]` prompt slot and its `WorkingMemoryState` dataclass, tracking current project and active artifacts across turns; schema, test suite, and open items (Tier 2 fields, render wiring). |
| 10 | [Semantic Search-Intent Classifier](docs/architecture/10-semantic-search-intent-classifier.md) | Authoritative | 2026-06-28 | Replaces literal-keyword web-search gating with an embedding-similarity classifier, added after a confirmed false-negative incident; covers design decisions, live re-verification, and a running log of threshold-tuning open items. |
| 11 | [Session File Attachments](docs/architecture/11-session-file-attachments.md) | Authoritative | 2026-08-01 | User-uploaded files (and, since 2026-07-21, pinned wiki pages) injected into every subsequent prompt via Slot SF for the session's duration, bypassing planner routing and wiki ingestion entirely; backend cache, API endpoints, UI, and prompt-assembly integration. §11.8: `SessionFile.source` distinguishes an uploaded file from a pinned wiki page, labeled "(from the vault)" in-prompt; `POST /chat/pin-wiki-page` validates against disk, not the graph index. New 2026-08-01: Open Item 4 (PDF/image support) RESOLVED — see §22; `ALLOWED_EXTENSIONS` widened, no other change to this module since OCR happens upstream in `main.py` and content arrives here as plain text. |
| 12 | [Chat History Tab](docs/architecture/12-chat-history-tab.md) | Authoritative | 2026-08-14 | Durable, searchable, user-manageable persistence of chat turns, distinct from both episodic memory and session files; schema, backend API, frontend, and live verification. Open Item 4 closed 2026-07-21: `/history` retired outright (superseded by §20's `/episodes`, retention control already lived independently on `/settings`) — see §20.7. Open Item 1 closed 2026-07-31: `chat_history_settings` renamed to `retention_settings` and became a shared global TTL for both `chat_turns` and episodes, with real enforcement (`MemoryManager.sweep_expired_memory()`) — see §20.10. Updated 2026-08-14: fixed stale-context bug where "New chat" (rotates `conversation_id`) left backend working memory keyed on the page-load-scoped `SESSION_ID`, leaking the prior conversation's turns into the model's prompt — `ControllerAgent._memory_key()` now prefers `conversation_id` over `session_id`. |
| 13 | [Localist CLI](docs/architecture/13-localist-cli.md) (`start_localist.sh`) | Authoritative | 2026-08-30 | The single-lifecycle launcher managing backend, `localist-mcp`, and the UI dev server together (start/stop/log-tail as one unit), including the dev-server-warmup fix for a chat-scroll regression. Updated 2026-08-30: `backend/` restructured into an installable `localist` package (`src/` layout, `pip install -e .`) — uvicorn targets are now package-qualified (`localist.main:app`, `localist.mcp_server.main:app`), resolved via the editable install rather than `cwd`; reload-dir/exclude paths updated to match. |
| 14 | [localist-mcp / MCP Tool Layer](docs/architecture/14-localist-mcp-tool-layer.md) | Authoritative | 2026-07-31 | The MCP tool server exposing `fetch_url`, `web_search` (provider-agnostic — LangSearch/Brave via `SEARCH_PROVIDER`, plus an Ollama Cloud second tier via `WEB_SEARCH_PROVIDER`), `generate_chart`, `news_search`, `github_search`/`github_read`/`github_release`, `ollama_web_search`/`ollama_web_fetch`, and file-op tools; `MCPToolDispatcher`, configuration, port topology, and test coverage. New 2026-07-31 (§14.15/§14.16, `ollama-web-search-mcp-tool-scoping.md`): Ollama Cloud as a second provider underneath `web_search` (`WEB_SEARCH_PROVIDER=brave` default with automatic fallback on Brave empty/failure, or `=ollama` to run without a Brave account at all — zero Planner changes, `tools_to_call` stays `["web_search"]`) and a fallback-only tier underneath `url_fetch` (no primary mode). Both branch inside the shared per-call helpers (`_execute_web_search_query()`, `_run_url_fetch()`) rather than any individual caller, so `news_search`'s Brave-fallback tier and the research loop transparently gained coverage too. Two regressions caught and fixed during build: the fallback firing even on a total localist-mcp outage (doubling the failure), and a test making a live authenticated Ollama call because its fixture didn't blank the new key — the latter also exposed and fixed a `controller_agent.py` Step 3b tool_name-matching gap identical to one `news_search` hit before. Live-verified against the real Ollama Cloud API for both tools, including catching and correcting an initially-invalid `OLLAMA_API_KEY` (401) before a clean end-to-end pass. Full suite 1390 → 1402 → 1406 passed. Same-day follow-up: closed a flagged-not-fixed gap in
`backend/tests/conftest.py`'s autouse env-isolation fixture — `OLLAMA_API_KEY`/`WEB_SEARCH_PROVIDER`
added to its stripped-vars tuple, closing the same class of live-API-leak risk regression #2 above
already confirmed once through a different (subprocess-based) fixture. New 2026-07-30 (§14.14): backend log readability — `mcp.client.sse` (full raw JSON-RPC frames, including the complete tool schema list, logged at DEBUG by the `mcp` SDK itself) and `httpcore`'s per-connection trace events (`connect_tcp.started`, etc., across its `http11`/`http2`/`connection`/`proxy`/`socks` children) were drowning out real signal under `LOCALIST_LOG_LEVEL=DEBUG`; fixed with two explicit per-logger `setLevel(INFO)` floors in `backend/main.py` right after the existing root `logging.basicConfig()`, leaving each logger's own WARNING/ERROR untouched and `httpx`'s one-INFO-line-per-request logging alone (real signal, not noise). Backend-process-only — `mcp.client.sse` only exists on the MCP client side. New 2026-07-30 (§14.13): `hacker_news_search` — a sixth crawl-style tool (Algolia's HN Search API), the chat-callable counterpart to the new Hacker News Live Feed panel block (§7.21); Planner's `_priority3_hacker_news` gate had to be ordered *before* P3-news specifically, since every "hacker news"-shaped phrase also contains the bare word "news"; `url`-pinning parity with `news_search` (§14.10) backs the panel's new "Ask about this" button. Live testing surfaced and fixed a real fabrication bug: the model inventing plausible-sounding Hacker News commentary from a bare comment count, with no real comment text ever supplied — fixed with `fetch_top_comments()` (Algolia's separate item API, the only endpoint that returns comment bodies), fetched only for the pinned single-story case. Full suite 1330 → 1371 passed. New 2026-07-29 (§14.12): `github_release(owner, repo, tag=None)` — release notes fetch (latest or a specific tag, with a "v"-prefix retry since repos are inconsistent about it), closing a real gap `github_search`/`github_read` couldn't cover (GitHub's Search API has no release index; Contents API isn't releases). Keyword-routable with no owner/repo named at all: `MCPToolDispatcher._run_github_release()` chains its own `github_search` call to resolve a bare project name from the top hit, same "search then fetch a specific record" shape `_enrich_top_result()` already established, both `ToolResult`s sharing a `workflow_id`. New `_GITHUB_RELEASE_KEYWORDS`/`_priority3_github_release` Planner gate, checked before P3-github, with a chart-keyword guard (a release-shaped phrase is common as a chart's *subject*, e.g. "chart the latest release notes," and must not hijack that compound). Live chat testing surfaced and fixed two real bugs beyond what mocked tests caught: a query-derivation heuristic leaving a bare "the" behind on certain phrasings, and a repo-resolution regex grabbing an unrelated URL embedded in a hit's own description before its actual bracketed repo-URL line (same latent class of bug `_extract_first_url()`'s enrichment path still has, unfixed, out of scope). Full suite 1284 → 1315 passed, including two pre-existing tests updated (not "fixed as regressions") since `_GITHUB_RELEASE_KEYWORDS` firing on "release notes" is by-design new behavior, not a bug. New 2026-07-29 (§14.11): `github_search`/`github_read` — public-repo GitHub REST reads (search, and README/file/directory content) for on-demand crawling during chat, the GitHub counterpart to `news_search` minus a fallback tier; `GITHUB_TOKEN` is optional (unlike every other key in this file) since both are public reads that work unauthenticated at a lower rate limit. Routed via a new Planner `_priority3_github` keyword gate (§4.2); `github_read` is reachable only via a caller-supplied `context["github_repo"]` pin, same convention as `news_search`'s URL-pinning (§14.10) — no frontend caller exists yet. Live-verified against real GitHub REST traffic, both unauthenticated and with a real token. New 2026-07-25 (§14.3): `MCPToolDispatcher._enrich_top_result()` — `web_search`/`news_search` now follow up a successful result with a `fetch_url` call on a result URL (excerpt capped at `_ENRICH_EXCERPT_CHARS=3000` chars, silent-fail on a blocked/slow fetch, `workflow_id`-stamped only when enrichment actually happens) — addresses Brave's `description` field being an inherently short meta-description that no truncation-cap change alone could fix. Two same-day follow-ups after live testing: (1) a bot-blocked (HTTP 403) top URL cost the whole enrichment, so it now tries up to `_ENRICH_MAX_ATTEMPTS=3` candidate URLs, in result order, before giving up, rather than quitting after the first failed fetch; (2) the original 2000-char excerpt cap was landing mid-paragraph before real added information on a real article, so it was raised to 3000. New 2026-07-25 (§14.2): `web_search` and `news_search` now build `result_text` via a shared deterministic formatter, `mcp_server/search_format.py` — a `Title — Source — Summary` bullet shape (source derived from URL when a provider has no publisher field) replacing each tool's own ad hoc bullet string; per-result summary caps raised from a flat 300 chars to 700 (web) / 600 (news) chars, paired with the Slot 5 `_CEIL_TOOL` raise in §3. New 2026-07-23 (§14.10): `news_search` gained an optional `url` param that pins its NewsAPI results to one already-known article (filtering to the exact URL match and, only in that case, surfacing NewsAPI's previously-discarded `content` field) — built for the new Live Feed "Ask about this" feature (§7.16) so a clicked article's own detail is looked up specifically rather than a fresh query that could resolve to a different story; additive and `None`-default, every existing caller unaffected. New 2026-07-22 (§14.9): `news_search` — a sixth tool, NewsAPI.org-backed, purpose-built for news-shaped queries the generic `web_search` API has no freshness/source concept for, with a tier-2 fallback to the existing Brave-backed `web_search` tool (no new provider integration needed for the fallback) on a NewsAPI miss or error, tagged `tool_name="news_search:brave_fallback"` for provenance. Live-verified against real NewsAPI/Brave traffic: a real query returned dated articles confirming the dev-tier 24h delay doesn't cause false misses, and a forced nonsense-query miss correctly exercised the real fallback chain end-to-end. Full suite 1042 → 1059 passed. New 2026-07-20 (§14.8): `generate_chart` — a fifth tool built diagnostic-first (measured 66.7% chart-argument-extraction MATCH rate on gemma-4-e4b-it-4bit via few-shot prompt + bracket-balanced envelope repair + one temperature-bumped retry, 12.1% residual failure accepted by design), promoted to production as `backend/chart_tool_schema.py`/`backend/json_envelope_repair.py` + `MCPToolDispatcher._run_chart()`, with a deliberate accepted-failure design (a failed chart extraction returns no `ToolResult` at all — not even an error one — so the turn just degrades to prose) that's a documented deviation from every other tool in this file. Live-verified end-to-end through the real running stack (planner gate → extraction pipeline → MCP tool → frontend render), including the degradation path. Same-day follow-up: closed a `_CHART_KEYWORDS` coverage gap live testing caught, and logged a new Open Item 2 repro (§14.7) — a model falsely claiming `"(Source: Tool output)"` for a chart that never rendered while the gap was still open — with a narrow, documented-as-partial mitigation (`controller_agent._strip_false_tool_attribution()`) that does not resolve Open Item 2. |
| 15 | [P6-Fallthrough Classifier & lookup_request Guard](docs/architecture/15-p6-fallthrough-classifier.md) | **Draft** (feature-flagged, not fully live) | 2026-07-08 | Two feature-flagged mechanisms — a shadow-mode classifier for tool-need fallthrough and a resolved-context guard for `lookup_request` — running live for trial data collection but not yet reflected in §4.2's routing tree. |
| 16 | [Runtime Backend Layer](docs/architecture/16-runtime-backend-layer.md) (`BaseRuntimeClient`) | Authoritative | 2026-07-31 | §16.13 (2026-07-31, decision record, no code changes): scoped oMLX v0.5.4rc1's new "Context Benchmark" Admin API as a candidate to feed §16.10/§16.11's `max_model_len` budget from a fresher, Localist-triggered measurement — investigated live against a real oMLX 0.5.4rc1 instance and **rejected**. The routes are real (`/admin/api/bench/context/*`, confirmed via `/openapi.json` and by reading the installed package source, since every response schema is untyped) but every successful run auto-applies its result to the model's own settings with no dry-run option, force-unloads every other loaded model as a side effect, 500s on a malformed request instead of a clean 4xx, and gates on session-cookie auth rather than the bearer-token scheme this codebase's runtime clients already use — assessed low-to-medium stability confidence (lives under `/admin/api/`, not the versioned `/v1/` surface §16.10 reads from). Localist stays a consumer-only of oMLX's self-reported `max_model_len`, unchanged; no new client, auth handling, or polling logic added. §16.4 further extended (2026-07-22): a live instance hit `sqlite3.OperationalError: table chat_turns has no column named embedding` despite `schema_version=9` already being current — the v8→v9 `ALTER TABLE` never actually ran on that DB, the same class of drift `_check_embedding_provenance()`'s existing self-heal was written for, just on a different table/column. Fixed with a new unconditional self-heal in `_init_db()` (checks/adds the column on every startup, not gated by embedding config, since `add_chat_turn()`'s `INSERT` always names it) — live-verified firing for real via `--reload` against the user's actual running backend, no restart needed. §16.4 extended (2026-07-21), Episode Browsing UI Phase 1: `embedding_provenance` gains a third store, `'chat_turns'` (schema v9 adds an `embedding BLOB` column to `chat_turns`). `add_chat_turn()` now embeds `content` (truncated to 500 chars) whenever `embed_fn` is configured; `'chat_turns'` follows the same never-automatic path as `'corpus'` (arbitrarily large under the `"forever"` eviction preset) via its own `self._chat_turns_stale` flag and `MemoryManager.reembed_chat_turns()` (`POST /memory/reembed-chat-turns`). `MemoryManager.get_chat_turns()` gains `mode="semantic"` (full-table cosine scan, no pre-filter column the way `document_index`'s `token_set` provides one) and `min_score`, exposed via `GET /chat/history?mode=semantic`; falls back to keyword/FTS when `embed_fn` is unavailable or the store is flagged stale, same fail-safe posture as `'corpus'`/`'episodes'`. §16.12 (2026-07-19): `OMLXRuntimeClient` now sends a real multi-turn `messages` array (system message, one entry per working-memory turn, current query last — mirroring oMLX's own web UI) instead of packing everything into one flattened user message, via a new oMLX-only `working_memory_turns` keyword on `infer()`/`infer_stream()` (Ollama/Foundry untouched — Protocol conformance checks member presence, not signatures). Also added distinct handling for oMLX's "Prompt too long" 400 (`server.py`'s `validate_context_window()`): drops the single oldest working-memory message and retries once; a second such 400 raises a clear, distinguishable error rather than looping. §16.11 (2026-07-19): retired the §16.8/§16.9 RAM-tiered `LOCAL_PROFILE` lookup outright (it measured the wrong layer — oMLX manages its own paged KV cache/memory per-machine, independent of host RAM) in favor of budgeting directly off §16.10's `max_model_len`: `working_memory_tokens = max_model_len − 27,000` (floored at 300), `working_memory_limit = None` on both tiers now (the local 5-turn cap is gone). `profile_for()`'s signature changed from `(is_local: bool)` to `(runtime: object)` since the budget now depends on a second live value that can change after a runtime-backend swap. §16.10 (2026-07-19): `OMLXRuntimeClient.health_check()` now also reads oMLX's `max_model_len` field (vLLM-compatible extension to `GET /v1/models`) for the active chat model, caching it onto `self.max_model_len` with an 8192-token conservative fallback when null/missing. §16.9 (2026-07-19): removed `total_context_tokens`/Ollama `options.num_ctx` after `diagnostics/reports/ollama_cloud_num_ctx_findings.md` confirmed it has zero observed effect on Ollama (local or cloud) — the real ceiling is always each model's native context length; corrects §16.7's now-outdated description of `num_ctx` as functional. §16.8 (2026-07-19): investigated whether `LOCAL_PROFILE`'s 300-token/5-turn working-memory ceiling could be safely raised — live-measured on both local backends (oMLX, local Ollama) using real `conversation_log` text and macOS `top` memory-footprint sampling (psutil RSS confirmed to under-report Metal/unified-memory footprint by ~40x on Apple Silicon, a real methodology pitfall now documented in `context_profile.py`). Result at the time: no safe increase (full data: `diagnostics/reports/local_working_memory_ram_findings.md`) — since superseded by §16.10/§16.11's max_model_len-based budget, not re-measured. Added `context_profile.check_local_ram_headroom()`, a startup warn-only guard (mirrors `lifespan()`'s existing reachable/not-reachable pattern) that logs when real headroom is already thin on the active machine, gated on the runtime's `is_local` flag — now purely observability, no longer feeding `working_memory_tokens` (§16.11). The structural-typing `BaseRuntimeClient` protocol and `runtime_factory.create_runtime()` swap-only backend selection; covers the oMLX, MLX-LM/Foundry, and Ollama backends. §16.7 (2026-07-18): every client now exposes a public `is_local: bool`, set once at construction — always `True` for oMLX/Foundry, derived for Ollama from the chat-model tag's `-cloud` suffix (not `base_url`, which is identical for local and cloud Ollama models). This is the single flag `context_profile.py`'s local/cloud `ContextProfile` ceilings key off (Working Memory turn/token caps consumed by `ControllerAgent`/`PromptBuilder`, see §3 Slot 6). §16.4: `OllamaRuntimeClient.embed()` implemented (real `/api/embed`, no longer a stub), the three-tier runtime-backend/MLX/keyword-only embedding-source precedence (platform-gated: MLX `EmbeddingEngine` only attempted on Apple Silicon), and a chat-model fail-fast `ValueError` replacing an unsafe hardcoded default — live-verified end to end with a cloud chat model and a local embedding model sharing one Ollama daemon. §16.4 open item confirmed 2026-07-16: embedding-model-aware thresholds are a real gap, not normal variance (`lookup_request` scored 0.7119 vs 0.578 for the identical utterance across two embedding models) — fixed on the Planner side via a `_TUNED_EMBEDDING_MODEL` guard that disables semantic search-intent gating on a model mismatch rather than silently degrading it, with `main.py` deriving and threading the active embedding model name through every `ControllerAgent` construction site. The identical gap on `MemoryManager`'s corpus/episodic cosine scoring, named as the follow-up above, is now also fixed: a new `embedding_provenance` table (schema v8) tracks which model produced the stored vectors per store (`'corpus'` | `'episodes'`), with a migration-safe seed-not-mismatch rule for pre-existing embedded data and no provenance row. On a genuine mismatch, `'corpus'` is flagged `_corpus_stale` (falls back to keyword-only scoring in `query_corpus()`, cleared only by the new manual `MemoryManager.reembed_corpus()` / `POST /memory/reembed`, since wiki/raw corpora can be large/expensive) while `'episodes'` auto-re-embeds in place at construction time (small, bounded cost) — the split decided for this follow-up. §16.3/§16.5: a real backend-level live switch — `POST /settings/runtime-backend`, per-backend chat-model pinning, `.env` persistence, health-check-before-swap — built and live-verified 2026-07-15 against real oMLX/Ollama/Foundry services, including the in-flight-request-safety claim; the `embed_fn`-staleness concern raised in review turned out to already be correct by construction and was hardened (public `MemoryManager.embed_fn` property, inline comments, two identity-check tests) rather than needing a fix. §16.6: the two remaining open items closed — `_runtime_switch_lock` is now an `asyncio.Lock` (a `threading.Lock` held across `await` was freezing the whole event loop, not just serializing requests, confirmed by a regression test that deadlocks against the old code), and the Settings UI (`localist-ui`, §7.10) is now wired to all three endpoints — live switching, per-backend chat-model dropdown/pinning, and a same-backend-no-op guard, gated by a confirm dialog. |
| 17 | [WikiAgent Diff-Target Path](docs/architecture/17-wiki-agent-diff-target.md) | Authoritative | 2026-07-28 | Standalone `diff_target` context field letting `wiki_agent` propose/apply a diff against an existing wiki page with no raw file involved; Planner Priority 1b routing (reusing P3c's `resolve_graph_target()`); the content-based `apply_unified_diff` hunk-location fix; a review-then-apply chat UI (`POST /wiki/apply-diff`, `metadata.pending_diffs`); and a pre-write snapshot safety net (`wiki/.snapshots/`, 30-day TTL via prune-on-write + startup sweep, unified `wiki_maintenance.log` audit trail) closing the prior no-rollback gap — all live-verified end to end. §17.12 (2026-07-28): closed the last open item — generalized `_extract_hunk_lines()`/`apply_unified_diff()`'s bullet/diff-marker collision recovery (previously removed/added-lines-only) to also cover the mirror case on unchanged context lines, via a content-based disambiguation (a genuinely dropped bullet leaves no space after the stripped marker char; a dropped context marker over a bullet does) and a generalized ordered-recovery-mode loop in place of the old two-tier try/except. §17.11 (2026-07-21): closed the "multi-diff turns" open item — investigated while scoping the Episode Browsing UI's diff detail view and found every layer (`WikiAgent` → `_build_wiki_diff_result` → `mark_diff_applied` → `POST /wiki/apply-diff` → `ChatPanel.svelte`'s per-`page_name`-keyed rendering) already operates on the full `pending_diffs` list independently; nothing needed generalizing. Verified, not fixed, with 3 new end-to-end tests (2 diffs sharing one turn, applying one leaves the other's status/on-disk content untouched). §17.10 (2026-07-21): Priority 1c short-circuits diff_target resolution to a single pinned wiki page's stem, folds in a Priority-3 tool signal (e.g. url_fetch) into the same compound plan, and threads the dispatched tool's result into the diff prompt via a new `tool_context` context key. |
| 18 | [Research Loop](docs/architecture/18-research-loop.md) | **Draft** (feature-flagged, off by default — but explicitly invokable via `/research` regardless, §18.2) | 2026-07-21 | §18.10 (2026-07-21), Episode Browsing UI Phase 2: `ToolResult` gains a `workflow_id` field; `_run_research_loop()` generates one id per call and stamps it on every `ToolResult` it produces (including the synthetic exhaustion result), so `controller_agent.py` can group them into `metadata["workflow_id"]`/`metadata["workflow_steps"]` (omitted entirely on non-research turns) for the Episode Browsing UI's step-chain view. A bounded search/evaluate/reformulate/fetch cycle (`MCPToolDispatcher._run_research_loop`) that Planner's `research_intent` semantic group upgrades an already-firing `web_search` into, for price/spec-lookup queries a single search snippet can't resolve; gated behind `LOCALIST_RESEARCH_LOOP_ENABLED` (`false` in `.env.example`) and `_RESEARCH_INTENT_THRESHOLD=0.65`. Covers the negative-filter redesign (post-scoring conflict resolution + a documented single-inference-call exception to Planner's no-inference-in-P1-4 rule), the loop's iteration cap/repeat-guard/synthetic-failure-result mechanics feeding `controller_agent.py`'s Step 3b corpus fallback, and the `_RESEARCH_CLASSIFIER_TIMEOUT=15s` override added after a live cloud-side stall. Extensively live-tested against real Brave Search/Ollama Cloud traffic, surfacing and fixing three real bugs (bracket-wrapped URL parsing, a silent-failure NDJSON stream bug in `ollama_runtime_client.py`, and the classifier-timeout tuning) — not yet enabled by default pending more live use. New 2026-07-20 (§18.2): a Priority 0 `/research` slash command (§4.2) forces the loop regardless of the ambient flag — live-verified running with the flag off. Also new 2026-07-20 (§18.9): an 18-query, 7-category live QA pass (`diagnostics/reports/research_loop_qa_assessment_2026-07-20.md`) found the pricing/spec gate's dominant failure mode was accepting any pricing-shaped content as sufficient regardless of relevance to the actual question (0/3 honest-exhaustion rate on deliberately-unanswerable queries); fixed by making `_evaluate_pricing_gate()` relevance-aware (now takes the original instruction, not just the candidate text). Delta re-run on the 6 clearest false positives: 4 improved, 2 unchanged (narrower gate gaps, not regressions), 0 regressed — see §18.8 for the two new open items this surfaced and the QA pass's own scope caveats. |
| 19 | [OKF Wiki Alignment](docs/architecture/19-okf-wiki-alignment.md) | Authoritative | 2026-07-21 | Reconciles the wiki corpus with Google's OKF (Open Knowledge Framework) spec, as adopted by LangChain's OpenWiki 0.2 — closing a pre-existing three-way front-matter drift (`SCHEMA.md`'s placeholder vs. `wiki_agent.py`'s actual `_EXAMPLE` template vs. real pages on disk) rather than adding a fourth inconsistent convention. New `META_WIKI_FILENAMES` shared constant (`wiki_doc.py`) excludes `index.md`/`logs.md`/`MEMORY.md` from corpus indexing, graph nodes, and the wiki-pin picker — also closing a pre-existing `MEMORY.md` exposure gap. `index.md`/`logs.md` are deterministically regenerated in `WikiAgent._finalize()`/`apply_pending_diff()`, never model-authored. `description`/`tags` are model-generated at write time going forward; a one-time `backfill_okf_frontmatter.py` script brought all 7 existing pages up to the new convention. |
| 20 | [Episode Browsing UI](docs/architecture/20-episode-browsing-ui.md) | Authoritative | 2026-07-31 | New three-pane (Filters / Episode list / Episode detail) `/episodes` view treating `chat_turns` as the browsing spine (Path B) and `episodes` as a read-only annotation overlay, rather than extending `VALID_EPISODE_TYPES` (Path A rejected). Backend: `chat_turns` semantic search (schema v9 embedding column, `'chat_turns'` embedding-provenance store, `get_chat_turns(mode="semantic")`, date-range/has-tool-result filters — §16.4/§20.2); a `workflow_id` correlation key stamped on every `ToolResult` a research-loop run produces, surfaced as `metadata["workflow_steps"]` (§18.10/§20.3); verification (not a fix) that multi-diff turns already worked end to end (§17.11/§20.4); a `task_id` filter on `list_episodes()`/`count_episodes()`, originally used for the per-turn "related memory" overlay (§20.5) — **superseded 2026-07-24**: that overlay now calls a new `GET /memory/episodes/related` semantic-similarity endpoint instead (full detail at §2.13 of the Episodic Memory Schema doc), the `task_id` filter itself unchanged. Frontend: new route/store/five components, including `DiffBlock.svelte` extracted from `ChatPanel.svelte` so both call sites share the same (already multi-diff-correct) rendering/apply logic. `/history` retired outright — superseded, no functionality lost (§12.7/§20.7). Backend test suite 987 → 1042 passed; no frontend test framework exists in this repo, verified via `svelte-check`/`vite build` only — no live-browser verification performed (§20.9). **2026-07-31 (§20.10):** episodes now get a global retention TTL, shared with chat_turns under one renamed `retention_settings` preset — episodes soft-retract (`status='retracted'`) rather than delete, enforced by a new startup-only `MemoryManager.sweep_expired_memory()`; closes §12.7's Open Item 1. **Same day (§20.11):** the reversal path §20.10 claimed but didn't build — `EpisodicMemoryWriter.reactivate()`, `POST /memory/episodes/{id}/reactivate`, and a new "Retracted" filter + "Reactivate" button in `EpisodesPanel.svelte` (full detail at §2.5 of the Episodic Memory Schema doc). |

| 21 | [Multimodal Image Upload — oMLX OCR Routing (Scope)](docs/architecture/21-multimodal-image-ocr-scope.md) | **Rejected** (2026-08-01, superseded by §22) | 2026-08-01 | Outline for letting users attach images in the chat panel and routing them to oMLX's Gemma 4 (native OCR/vision) — no dedicated OCR service. Never built: after this outline was completed, a second alternative (a dedicated, inference-engine-agnostic local OCR service) was scoped and chosen instead, mainly to avoid tying the attach feature to one runtime backend and avoiding a new multimodal wire-format contract across `PromptBuilder`/`BaseRuntimeClient`. See §22.8 for the full comparison. Kept in place (not moved to `archive/`) as a documented record of the alternative considered. |
| 22 | [Local OCR Service (Apple Vision + PyMuPDF)](docs/architecture/22-local-ocr-service.md) | Authoritative | 2026-08-03 | New 2026-08-01 (§22.10): documented as a known, by-design limitation (not a bug) — this is text extraction only, via Vision's `VNRecognizeTextRequest`; a photo with no visible text (live-reported: a movie poster, `Runaways 2021.jpg`) correctly finds nothing and rejects with the same "no readable text detected" message a blank scan gets, since the tool has no way to distinguish the two. General image captioning/description is a different capability, out of scope here, and would need its own scoping pass. Chat uploads accept images (incl. HEIC) and PDFs, OCR'd to plain text once at upload time by a new `ocr_extract` MCP tool on `localist-mcp` (§14) — Apple Vision framework for images, PyMuPDF for PDF text-layer extraction with a per-page rasterize+OCR fallback for scanned PDFs — entirely independent of whichever chat runtime backend (oMLX/Ollama/Foundry, §16) is active. Resolves §11.6's Open Item 4 and §3's Slot SF PDF/image note, superseding the oMLX-native approach scoped (and rejected) at §21. Once extracted, OCR'd content is indistinguishable from a text upload to `session_files.py`/`PromptBuilder` — no changes needed to either, no separate image cache, no per-turn resend cost. `main.py`'s `POST /chat/files` routes by extension before its UTF-8-decode path; `ChatPanel.svelte` shows an "Extracting text…" state during the real (non-mocked) OCR latency. Two real bugs caught by testing during the build (§22.9): a falsy-zero `max_pdf_pages=0` bug, and a scanned-PDF-with-no-readable-text case that was slipping past the near-empty-result rejection via a bare page-marker string. Full suite 1406 → 1443 passed. **2026-08-03:** §22.8's explicitly-flagged gap (`WikiAgent`'s oMLX-only PDF/image ingest path) closed — see §24. |
| 23 | [`auditlog-core` — Installable Audit-Logging Library (Scope)](docs/architecture/23-auditlog-core-scope.md) | **Draft — deferred** | 2026-08-02 | Scoping-only open concern: extract `claude-impact-lab-demo`'s already-built audit-logging pipeline (`risk_classifier.py`, `interactions` SQLite schema, classify→insert→markdown pipeline) into a standalone, provider/runtime-agnostic installable library so Localist can add local audit logging to its own chat turns via a plain-data `.record()` call. Resolved the standalone-repo-vs-extracted-module question (standalone repo) and identified the concrete Localist integration point (`_persist_chat_turn()` call sites in `backend/main.py`, §23.6) and a real schema gap (no correlation column for `task_id`/`conversation_id`, §23.5) against the actual codebases. Explicitly deferred — no code changes to either repo — until after the 2026-08-06 Claude Impact Lab demo, whose own point is proving `claude-impact-lab-demo` works against the real Claude/OpenAI APIs. |
| 24 | [WikiAgent Ingest Alignment with the Local OCR Service](docs/architecture/24-wikiagent-ocr-ingest-alignment.md) | Authoritative | 2026-08-03 | Closes §22.8's explicitly-flagged gap: `WikiAgent`'s PDF/image `raw_path` ingest previously depended on oMLX's `infer_with_file()` (MarkItDown-based, not real OCR) and failed outright on Ollama/Foundry (`read_text_file()`'s UTF-8 decode raising on binary content). Now routes OCR-eligible raw files through the same `ocr_extract` MCP tool chat uploads use (§22.2) — extracted text always takes the `infer()` string-prompt path, never `infer_with_file()`, so ingestion is identical across all three backends. `mcp_server/ocr.py`'s extension→mime map made public (`OCR_MIME_BY_EXTENSION`) as the shared source of truth; `POST /files/upload`/`GET /files/raw` widened to accept/list OCR-eligible files (saved as-is, OCR happens lazily at ingest, not upload); `Sidebar.svelte`/`FileBrowser.svelte`/`fileSelection.ts` widened to match, with a new preview-unavailable state for binary raw files. Bug caught by live testing, not inspection (§24.6): `_resolve_raw_path()`'s pre-existing `.md`/`.txt`-only gate rejected OCR-eligible files before the new routing logic was ever reached; widened alongside the rest. Live-verified against the real running stack (real Vision OCR, real Ollama inference, real MCP round trip) via `POST /task` and `POST /files/upload`. Full suite 1443 → 1460 passed, 0 failed. |

*Section 1 (System Identity) was folded into "Core Constraints" above — its original 17 lines didn't warrant a standalone file.*

---

## Keeping this index current

When you finish a change to any `docs/architecture/NN-*.md` file:

1. Update that row's **Last updated** date.
2. Re-read the **Summary** cell — rewrite it if the change was material, leave it if cosmetic.
3. Re-check **Status** — move to `Retired` (and relocate the file to `docs/architecture/archive/`) if the change makes the section dead; otherwise leave as `Authoritative` or `Draft`.

Do not add substantive content to this file directly — it stays index-sized so it's cheap to read in full every session.

---

*End of Localist Framework Canonical Architecture Specification (index)*
